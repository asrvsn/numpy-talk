#!/usr/bin/env python

import sys
import os
import argparse
import timeit
import time
import numpy as np

import kuramoto as km

@profile
def main(N: int, dtype: np.dtype):
	time.sleep(2)
	print('done')

if __name__ == '__main__':
	parser = argparse.ArgumentParser(description='2D lattice Kuramoto model')
	parser.add_argument('--dtype', type=str, default='float64', help='Precision')
	parser.add_argument('-N', type=int, help='Size of the problem')
	args = parser.parse_args()

	main(args.N, np.dtype(args.dtype))